---
defaults:
  - _self_
  - run: inference
  - model: code-gpt2-small
  - wandb: default
  - hydra: output_inference

run:
  base_path: null
  preprocessing_num_workers: 16
  seed: 42

device: cuda
use_wandb: False