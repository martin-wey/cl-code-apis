---
defaults:
  - _self_
  - run: codesearch
  - model: codebert
  - wandb: default
  - hydra: default

run:
  run_name: test
  base_path: null   # leave it to null
  seed: 42
  do_train: False
  do_test: False

device: cuda
parallel: False
use_wandb: True
