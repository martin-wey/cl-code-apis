---
defaults:
  - _self_
  - run: clm_pretraining
  - model: code-gpt2-small
  - wandb: default
  - hydra: default

run:
  run_name: clm_pretraining
  # leave base_path to null
  base_path: null
  preprocessing_num_workers: 4
  seed: 42

device: cuda
parallel: False
use_wandb: False