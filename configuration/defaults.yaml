---
defaults:
  - _self_
  - run: clm_pretraining
  - model: code-gpt2-small
  - wandb: default
  - hydra: no_output

run:
  run_name: run_clm_pretraining
  # leave base_path to null
  base_path: null
  seed: 42

device: cuda
parallel: False
use_wandb: False
